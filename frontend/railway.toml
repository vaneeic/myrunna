[build]
builder = "nixpacks"
buildCommand = "npm install && npm run build"
installCommand = "npm install"

[deploy]
# Angular build output goes to dist/frontend/browser
startCommand = "npx serve dist/frontend/browser -l $PORT"
